// // @ts-ignore
// import isNode from 'is-node';
// import { serverPort } from '../../config/config';

// export const getDomain = () => {
//   const domain = !isNode ? '' : `http://localhost:${serverPort}`;
//   return domain;
// };
