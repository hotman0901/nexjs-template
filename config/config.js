// const apiUrl = 'http://127.0.0.1:5050';
// const serverPort = 6100;

// const api = process.env.API_URL ? process.env.API_URL : (process.env.NODE_ENV !== 'production' ? apiUrl : '')
// const port = process.env.SERVER_PORT ? process.env.SERVER_PORT : serverPort;
// const cdn = process.env.CDN_URL ? process.env.CDN_URL : ''
// const env = process.env.NODE_ENV ? process.env.NODE_ENV : ''

// export {
//   // 如果沒有 api url 參數，且是開發模式下直接給 default
//   api as apiUrl,
//   port as serverPort,
//   cdn as cdnUrl,
//   env,
// }
